version: '3.8'

services:
  supabasenet:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - USE_HTTPS_REDIRECTION=false
      - PORT=8080
    restart: unless-stopped

